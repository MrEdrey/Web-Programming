<?php
$checker = 0;
$rectangle = 0;

fscanf(STDIN, "%d", $data);

while ($checker<>$data){
    fscanf(STDIN, "%f%d", $idc, $amount);
    if ($amount%2 <> 0){
        $amount -= 1;
        $rectangle += $amount;
        $checker += 1;
    }else{
        $rectangle += $amount;
        $checker +=1;
     }
}
printf ("%d", $rectangle/4);
?>